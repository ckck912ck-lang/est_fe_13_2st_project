# AI 작업 하네스

이 문서는 Codex, ChatGPT, Cursor 등 AI 도구로 코드를 생성하거나 수정할 때 반드시 지켜야 하는 작업 규칙입니다.
AI는 프로젝트 맥락을 이해하는 것뿐 아니라, 정해진 범위 안에서만 수정해야 합니다.

## 1. 작업 전 확인

AI에게 작업을 요청하기 전에 아래 문서를 함께 제공합니다.

- `docs/convention.md`
- `docs/qa-checklist.md`
- 현재 작업 대상 HTML/SCSS/JavaScript 파일
- 필요한 경우 `data/*.json` 파일

AI에게는 다음 내용을 명확히 전달합니다.

- 작업 목표
- 수정 가능한 파일 목록
- 수정하면 안 되는 파일 목록
- 완료 조건
- 확인해야 할 QA 항목

## 2. 수정 금지 규칙

AI는 아래 파일을 직접 수정하지 않습니다.

- `css/style.css`
- `css/style.css.map`
- `main` 또는 `deploy` 브랜치의 파일 직접 수정
- 작업 요청과 무관한 공통 모듈

스타일 수정은 반드시 `scss/` 하위 파일에서만 진행합니다.
`css/style.css`는 SCSS 컴파일 결과물로만 갱신합니다.

## 3. 작업 범위 제한

AI는 요청받은 작업 범위 안의 파일만 수정합니다.

예외적으로 아래 경우에는 범위 밖 파일 수정이 가능합니다.

- import 경로 수정이 반드시 필요한 경우
- 공통 모듈 수정 없이는 기능 구현이 불가능한 경우
- 명백한 오타, 깨진 링크, 잘못된 참조를 수정하는 경우

단, 예외 수정이 발생하면 작업 결과 보고에 반드시 이유를 적습니다.

## 4. JavaScript 작업 규칙

- 공통 재사용 코드는 `js/modules/` 또는 `js/utils/`에 작성합니다.
- 페이지 전용 조립 코드는 `js/pages/`에 작성합니다.
- `js/pages/` 파일에는 페이지별 실행 흐름과 작은 전용 코드만 둡니다.
- DOM 생성, 데이터 처리, 이벤트 연결은 가능하면 함수로 분리합니다.
- 함수 하나는 가능하면 하나의 목적만 수행합니다.
- `var`는 사용하지 않고 `const`, `let`을 사용합니다.
- 세미콜론을 사용합니다.

## 5. SCSS 작업 규칙

- 모바일 퍼스트로 작성합니다.
- 반응형은 `min-width` 기준으로 작성합니다.
- 컴포넌트 스타일은 `scss/components/`에 작성합니다.
- 레이아웃 스타일은 `scss/layout/`에 작성합니다.
- 페이지 전용 스타일은 `scss/pages/`에 작성합니다.
- `scss/style.scss`에서 `@use`로 불러옵니다.
- HTML에는 `css/style.css` 하나만 연결합니다.
- 선택자는 태그보다는 클래스 위주로 작성합니다.
- 다른 영역에 영향을 끼치지 않도록 선택자를 명확하게 구성합니다.

## 6. HTML 작업 규칙

- HTML5 표준 문법을 사용합니다.
- `<!DOCTYPE html>`을 선언합니다.
- `html` 태그에 `lang="ko"`를 명시합니다.
- 가능한 경우 `div` 대신 시맨틱 태그를 사용합니다.
- 이미지에는 `alt` 속성을 작성합니다.
- 버튼과 링크에는 접근 가능한 이름을 제공합니다.
- 폼 요소에는 `label`을 연결합니다.

## 7. AI 작업 요청 예시

```txt
아래 문서를 기준으로 작업해줘.

- docs/convention.md
- docs/ai-harness.md
- docs/qa-checklist.md

작업 목표:
product-list.html에서 products.json 데이터를 불러와 상품 카드를 렌더링한다.

수정 가능한 파일:
- js/pages/productList.js
- js/modules/renderProductCard.js
- js/utils/fetchData.js
- scss/pages/_product-list.scss

수정 금지:
- css/style.css
- css/style.css.map
- 작업 범위 밖의 HTML 파일

완료 조건:
- 상품 목록이 화면에 렌더링된다.
- 콘솔 에러가 없다.
- 360px 모바일 화면에서 레이아웃이 깨지지 않는다.
- 상품 클릭 시 상세 페이지로 이동한다.
```

## 8. 작업 후 보고 형식

AI는 작업 후 아래 형식으로 결과를 보고합니다.

```md
## 수정한 파일

-

## 구현한 내용

-

## 컨벤션 준수 여부

- `css/style.css` 직접 수정 없음
- 모바일 퍼스트 기준 유지
- JS 공통/페이지 코드 분리
- 콘솔 에러 확인

## 확인이 필요한 부분

-
```

## 9. 금지되는 작업 방식

- 요청하지 않은 전체 리팩토링
- 파일 구조 임의 변경
- 공통 컴포넌트의 무단 수정
- `css/style.css` 직접 수정
- 임의 라이브러리 추가
- 실제 인증, 결제, 서버 기능 구현처럼 프로젝트 범위를 벗어난 작업

## 10. 권장 작업 단위

AI에게 한 번에 너무 큰 작업을 맡기지 않고 아래처럼 나누어 요청합니다.

```txt
1. 데이터 fetch 유틸 작성
2. 상품 카드 렌더링 함수 작성
3. 상품 목록 페이지 연결
4. 필터 기능 추가
5. 정렬 기능 추가
6. 페이지네이션 추가
7. QA 체크리스트 기준 점검
```
